
/**
 * Getting Element
 * methods and api for getting elements
 * 
 */

 

 class GetElement {

    // get element by the selector
    getElement(selector) {
        return document.querySelector(select)
    }

    // get all element that has this selector
    getAllElement(selector) {
        return document.querySelectorAll(selector)
    }
 }